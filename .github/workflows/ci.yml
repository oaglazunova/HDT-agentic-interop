name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install flask requests python-dotenv mcp

      - name: Create .env
        run: |
          cp .env.example .env

      - name: Run pytest
        run: pytest -q

      - name: Smoke test (API+MCP)
        env:
          HDT_ALLOW_PLACEHOLDER_MOCKS: "1"
          HDT_API_BASE: "http://127.0.0.1:5000"
          MCP_CLIENT_ID: "MODEL_DEVELOPER_1"
          HDT_API_KEY: "MODEL_DEVELOPER_1"
          MODEL_DEVELOPER_1_API_KEY: "MODEL_DEVELOPER_1"
          HDT_VAULT_ENABLE: "1"
        run: |
          python scripts/smoke_mcp.py
