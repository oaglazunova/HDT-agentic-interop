[pytest]
addopts = -q
testpaths = tests
asyncio_mode = auto
# Ensure src-layout packages are importable when running pytest without
# an editable install (e.g., in bare CI jobs).
pythonpath =
    src
markers =
    integration: integration tests (opt-in; run with --run-integration)
